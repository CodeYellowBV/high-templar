sudo: false
language: python
python:
  - "3.4"
  - "3.5"
cache:
  pip: true
install:
  - ./setup.py install
  - pip install greenlet codecov coverage
script:
  - coverage run --source="high_templar" --concurrency="greenlet" setup.py test
after_success:
  - codecov
